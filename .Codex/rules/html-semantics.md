# Règle HTML - sémantique interactive

- Action utilisateur cliquable -> vrai `<button>`.
- Ajouter `type="button"` hors formulaire.
- Onglet simulé cliquable -> `<button>`, pas `<span>` / `<div>`.
- Onglet actif -> `aria-current="page"` si navigation.
- Garder les styles via classes existantes.
